# FinBridge 기획서

## 1. 프로젝트 개요

FinBridge는 보험사 금융 IT 인터페이스를 통합적으로 관리하기 위한 운영 플랫폼 MVP이다.

보험사는 금감원, 제휴사, 결제사, 신용평가사 등 다양한 외부 기관과 데이터를 주고받는다. 이 과정에서 REST API, SOAP, MQ, Batch, SFTP/FTP 등 서로 다른 프로토콜 기반의 인터페이스가 함께 사용된다. 그러나 이러한 인터페이스가 개별 시스템 단위로 분산 관리되면 운영 복잡도가 증가하고 장애 대응이 늦어질 수 있다.

본 프로젝트는 이러한 문제를 해결하기 위해 다양한 외부 연계 인터페이스를 단일 화면에서 등록, 실행, 모니터링, 제어, 재처리할 수 있는 통합관리 플랫폼을 구축하는 것을 목표로 한다.

---

## 2. 추진 배경

기존의 금융 IT 인터페이스 운영은 다음과 같은 문제를 가진다.

- 프로토콜별 운영 방식이 달라 담당자가 개별적으로 관리해야 한다.
- 장애 발생 시 어느 기관, 어느 인터페이스에서 문제가 발생했는지 빠르게 파악하기 어렵다.
- 실패 건에 대한 재처리나 상태 확인이 수작업으로 이루어질 가능성이 높다.
- 로그, 응답속도, 성공/실패 상태가 분산되어 운영 효율이 떨어진다.

따라서 운영자 관점에서 인터페이스를 공통 모델로 관리하고, 실행부터 상태 확인, 장애 대응까지 연결된 운영 흐름을 제공하는 플랫폼이 필요하다.

---

## 3. 프로젝트 목표

본 프로젝트의 목표는 다음과 같다.

- 다양한 외부 연계 인터페이스를 하나의 관리 모델로 등록한다.
- 인터페이스를 활성/비활성 상태로 제어할 수 있다.
- 운영자가 인터페이스를 수동 실행할 수 있다.
- 스케줄 기반 점검을 통해 인터페이스 상태를 주기적으로 확인한다.
- 실행 결과를 로그로 저장하고, 성공/실패/지연 상태를 시각적으로 확인할 수 있다.
- 실패 또는 지연 건에 대해 재처리를 수행할 수 있다.
- 향후 실제 REST, SOAP, MQ, Batch, SFTP/FTP 연동으로 확장 가능한 구조를 갖춘다.

---

## 4. 주요 사용자

### 4.1 운영자
- 외부 기관 연계 인터페이스 등록
- 인터페이스 활성/비활성 제어
- 상태 모니터링 및 장애 확인
- 실패 건 재처리

### 4.2 관리자
- 전체 인터페이스 현황 파악
- 로그 및 장애 추세 분석
- 운영 정책 반영 및 확장 계획 수립

---

## 5. 주요 기능

### 5.1 인터페이스 등록 및 관리
운영자는 기관명, API명, 프로토콜, 대상 URL을 입력해 새로운 인터페이스를 등록할 수 있다. 등록된 인터페이스는 목록 형태로 확인 가능하며, 활성/비활성 상태를 토글 방식으로 관리한다.

### 5.2 대시보드 기반 통합 모니터링
대시보드는 전체 인터페이스 수, 정상 처리 건수, 오류 건수, 지연 건수, 총 호출 건수 등의 요약 정보를 카드 형태로 제공한다. 또한 외부 기관 API 상태 테이블을 통해 최근 실행 결과를 직관적으로 확인할 수 있다.

### 5.3 수동 실행
운영자가 특정 인터페이스를 즉시 실행해 상태를 점검할 수 있다. 이는 자동 점검과 별도로 운영 개입이 필요한 상황을 지원하기 위한 기능이다.

### 5.4 자동 점검
활성화된 인터페이스를 대상으로 스케줄러가 주기적으로 상태를 점검하고, 실행 결과를 로그로 저장한다.

### 5.5 로그 조회 및 상세 확인
운영자는 최근 실행 로그를 조회할 수 있으며, 상태별, 프로토콜별, 키워드 기준 필터링과 개별 로그 상세 확인이 가능하다.

### 5.6 재처리
실패 또는 지연 상태의 로그에 대해 재처리 기능을 제공한다. 재처리는 기존 로그를 수정하는 방식이 아니라 새로운 실행 이력을 추가로 생성하여 추적 가능하도록 설계한다.

### 5.7 에러 메시지 관리
단순히 FAIL 또는 DELAY 상태만 기록하는 것이 아니라, 실패 원인과 지연 사유를 에러 메시지로 함께 저장해 운영자가 장애 원인을 빠르게 파악할 수 있도록 한다.

### 5.8 프로토콜별 실행 전략 분리
REST, SOAP, MQ, BATCH, SFTP, FTP를 동일한 CRUD 수준으로만 저장하지 않고, 프로토콜별 executor 분리 구조를 통해 향후 실제 연동 모듈로 확장할 수 있도록 설계한다.

---

## 6. 화면 구성

현재 UI는 사이드바 기반의 단일 페이지 대시보드 구조로 설계되어 있으며, 주요 메뉴는 다음과 같다.

- 대시보드
- API 등록
- 로그 화면

메인 화면에서는 카드, 차트, 상태 테이블을 통해 운영 상황을 한눈에 파악할 수 있도록 구성한다.

---

## 7. 기대 효과

- 분산된 인터페이스 운영을 단일 화면으로 통합
- 장애 발생 시 빠른 탐지 및 대응 가능
- 수동 실행과 재처리 기능을 통한 운영 효율 향상
- 프로토콜별 운영 복잡도를 공통 관리 모델로 완화
- 향후 실제 기관 연동 시스템으로 확장 가능한 기반 확보

---

## 8. 범위와 한계

본 프로젝트는 과제 특성과 개발 기간을 고려해 통합관리 플랫폼의 핵심 운영 흐름 구현에 초점을 맞췄다.

따라서 실제 외부 기관과의 전면적인 실연동보다는 다음 범위를 우선 구현했다.

- 인터페이스 등록 및 관리
- 상태 점검
- 실행 로그 저장
- 재처리
- 에러 메시지 및 상태 모니터링
- 프로토콜별 확장 가능한 구조 설계

즉, 본 프로젝트는 실무형 완성 시스템보다는 MVP 성격의 통합운영 플랫폼이며, 이후 실제 MQ 브로커, SOAP 서버, SFTP 서버, 배치 스케줄러와 연결할 수 있도록 발전 가능한 형태를 목표로 했다.

---

## 9. 결론

FinBridge는 보험사 금융 IT 인터페이스를 단일 화면에서 관리하기 위한 통합관리 플랫폼이다. 본 프로젝트를 통해 다양한 외부 연계 인터페이스를 등록, 실행, 모니터링, 제어, 재처리할 수 있는 운영 흐름을 구현하고자 했다.

또한 단순 조회 시스템이 아니라 운영자가 실제로 상태를 확인하고 장애에 대응할 수 있는 흐름 중심의 플랫폼으로 설계하여, 향후 실제 기관 연동 환경으로 확장 가능한 기반을 마련하는 것을 목표로 한다.