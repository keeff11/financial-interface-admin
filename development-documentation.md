# FinBridge 개발문서

## 1. 프로젝트 개요

본 시스템은 보험사 내부 시스템과 외부 기관 간의 인터페이스를 중앙에서 관리하기 위한 웹 기반 플랫폼이다.

백엔드는 Spring Boot와 JPA를 사용해 인터페이스 등록, 실행, 재처리, 로그 관련 API를 제공하고, 프론트엔드는 React 및 Next.js 기반으로 운영자용 대시보드와 등록/로그 화면을 제공한다.

본 프로젝트의 핵심 목적은 다양한 금융 IT 인터페이스를 하나의 공통 모델로 관리하고, 등록, 실행, 모니터링, 제어, 재처리 흐름을 하나의 플랫폼으로 통합하는 것이다.

---

## 2. 기술 스택

### 2.1 Backend
- Java
- Spring Boot
- Spring Data JPA
- Hibernate
- H2 Database

### 2.2 Frontend
- React
- Next.js
- JavaScript

---

## 3. 시스템 아키텍처

### 3.1 핵심 구성 요소
- Frontend: 대시보드, API 등록 UI, 로그 관리 UI
- Backend: 인터페이스 관리 API, 실행 API, 재처리 API, 로그 API
- Database: 인터페이스 마스터 정보 및 실행 로그 저장
- Scheduler: 활성 인터페이스 주기 점검
- Executor Layer: 프로토콜별 실행 전략 분리

### 3.2 처리 흐름
1. 운영자가 인터페이스를 등록한다.
2. 등록 정보는 데이터베이스에 저장된다.
3. 활성 상태인 인터페이스는 자동 점검 대상이 된다.
4. 운영자는 특정 인터페이스를 수동 실행할 수 있다.
5. 실행 결과는 로그 테이블에 저장된다.
6. 실패 또는 지연 시 에러 메시지가 함께 기록된다.
7. 운영자는 로그 화면에서 필터링, 상세 조회, 재처리를 수행할 수 있다.

---

## 4. 도메인 설계

### 4.1 ApiInfo
ApiInfo는 인터페이스 마스터 정보를 저장하는 엔티티이다.

주요 필드:
- institution
- apiName
- protocol
- targetUrl
- active

주요 역할:
- 인터페이스 등록 정보 저장
- 활성/비활성 상태 관리
- 프로토콜별 실행 대상 식별

### 4.2 ApiLog
ApiLog는 인터페이스 실행 결과를 저장하는 로그 엔티티이다.

주요 필드:
- status
- responseTime
- errorMessage
- triggeredBy
- retriedFromLogId
- createdAt

주요 역할:
- SUCCESS, FAIL, DELAY 상태 저장
- 응답속도 기록
- 실행 이력 관리
- 재처리 이력 추적

---

## 5. 백엔드 API 설계

### 5.1 기본 조회 및 관리 API
- `GET /api/summary`
- `GET /api/interfaces`
- `GET /api/api-info`
- `POST /api/register`
- `PUT /api/api-info/{id}/toggle`

### 5.2 운영 기능 API
- `POST /api/api-info/{id}/execute` : 수동 실행
- `POST /api/logs/{id}/retry` : 재처리
- `GET /api/logs/recent` : 로그 조회 및 필터
- `GET /api/logs/{id}` : 로그 상세 조회

---

## 6. 프론트엔드 구조

### 6.1 FinancialDashboard
메인 레이아웃 컴포넌트이다.  
사이드바, 헤더, 카드, 차트, 상태 테이블, API 등록 화면, 로그 화면을 조건부 렌더링으로 전환한다.

### 6.2 StatCards
요약 카드 컴포넌트이다.  
전체 인터페이스 수, 정상 처리 건수, 오류 건수, 총 호출 건수를 카드 형태로 표시한다.

### 6.3 ApiTable
인터페이스 상태 테이블 컴포넌트이다.  
기관명, API 이름, 프로토콜, 상태, 응답속도 등을 표시하며, 상태별 색상과 배지 형태로 시각화한다.

### 6.4 ApiRegistration
인터페이스 등록 화면 컴포넌트이다.  
기관명, API명, 프로토콜, 대상 URL을 입력받아 등록하고, 등록된 인터페이스의 활성/비활성 상태를 변경할 수 있다.

### 6.5 MonitoringLogs
로그 화면 컴포넌트이다.  
최근 로그 조회, 상태/프로토콜/키워드 기준 필터링, 로그 상세 조회, 재처리 기능을 제공한다.

### 6.6 Sidebar
대시보드, API 등록, 로그 화면 메뉴를 제공하는 내비게이션 컴포넌트이다.

---

## 7. 실행 로직

### 7.1 자동 점검
활성화된 인터페이스를 대상으로 스케줄러가 주기적으로 실행을 수행하고 결과를 로그로 저장한다.

### 7.2 수동 실행
운영자가 특정 인터페이스를 직접 실행해 즉시 상태를 점검할 수 있도록 구현했다.

### 7.3 재처리
실패 또는 지연 로그를 선택해 재처리할 수 있다. 재처리 시 기존 로그를 수정하지 않고 새로운 로그를 생성하여 실행 이력을 유지한다.

### 7.4 프로토콜별 executor 분리
프로토콜을 단순 문자열로만 저장하지 않고, REST, SOAP, MQ, BATCH, SFTP, FTP 별로 실행 전략을 구분할 수 있도록 executor 분리 구조를 적용했다.

---

## 8. 예외 및 장애 대응

시스템은 운영 관점에서 다음과 같은 예외 및 장애 대응 구조를 고려하였다.

- 실행 실패 시 에러 메시지 저장
- 실패 및 지연 상태 로그 재처리 지원
- 자동 점검과 수동 실행 분리
- 로그 기반 상태 추적 가능
- 환경에 따라 데이터소스 설정이 달라질 수 있음을 고려한 구조 설계

---

## 9. 현재 한계

- 외부 기관과의 실제 REST, SOAP, MQ, SFTP/FTP 연동은 미구현
- 프로토콜별 세부 설정 항목 부족
- 로그 분석 기능은 기본 필터 및 상세 조회 수준
- 인증 및 권한 관리 미구현
- 사용자별 운영 이력 관리 미구현

---

## 10. 향후 개선 방향

- MQ queue/topic, Batch schedule, SFTP remote path 등 프로토콜별 세부 설정 추가
- 실제 외부 시스템과의 연결 모듈 구현
- 실패 및 지연 로그 통계 시각화 고도화
- 사용자 인증 및 권한 관리 추가
- 운영 변경 이력 감사 로그 추가

---

## 11. 결론

FinBridge는 보험사 금융 IT 인터페이스 통합관리시스템을 목표로 개발한 MVP이다.

실제 프로토콜 통신을 모두 완전하게 구현하기보다는, 운영자가 다양한 인터페이스를 하나의 공통 모델로 관리할 수 있도록 등록, 실행, 모니터링, 제어, 재처리, 로그 관리의 핵심 흐름 구현에 초점을 맞췄다.

이를 통해 다양한 프로토콜의 인터페이스를 단일 화면에서 운영할 수 있는 기반을 마련했으며, 향후 실제 기관 연동 환경으로 확장할 수 있는 구조를 확보했다.